const userName: string = "tigui";

const sum = (a: number, b: number) => {
  return a + b;
}

sum(4,5)

class Person {
  constructor (age: number, lastName: string) {  }
}

const tigui = new Person(15, "Jourdan");

